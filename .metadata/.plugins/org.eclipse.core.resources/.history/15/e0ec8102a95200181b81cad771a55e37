package Ejer2;

public class MainTest {

	public static void main(String[] args) {
		Hilo hilo = new Hilo();
		Panel panel1 = new Panel("panel1", 620, 500);
		hilo.crearMatrices();
		hilo.suma();
		panel1.escribir_mensaje(hilo.escribir(hilo.getMatrizA()));
		panel1.escribir_mensaje(hilo.escribir(hilo.getMatrizB()));
		panel1.escribir_mensaje(hilo.escribir(hilo.getMatrizC()));
		
	}

}
